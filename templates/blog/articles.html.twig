{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{ category.name }}</h1>
    {% for article in articles %}
        <li><a href="/blog/{{ article.title }}">{{ article.title }}</a> : {{ article.content }}</li>
    {% endfor %}
{% endblock %}