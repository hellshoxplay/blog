{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{ category.name }}</h1>
    {% for article in articles %}
        <li>{{ article.title }} : {{ article.content }}</li>
    {% endfor %}
{% endblock %}